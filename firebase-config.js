/* ==========================================================================
   КОНФИГУРАЦИЯ FIREBASE ДЛЯ ЧАТА
   --------------------------------------------------------------------------
   Как настроить (один раз, ~5 минут):
   1. Зайди на https://console.firebase.google.com (войди аккаунтом Google).
   2. Нажми «Создать проект» → назови например «so2-chat» → создай.
   3. На странице проекта нажми «+ Добавить приложение» → выбери веб-иконку "</>".
   4. Скопируй блок 'firebaseConfig' со страницы и вставь его в переменную FIREBASE_CONFIG ниже
      ВМЕСТО значений по умолчанию.
   5. В левом меню консоли: "Realtime Database" → «Создать базу данных» →
      выбери регион → на шаге правил выбери «Начать в тестовом режиме» → «Включить».
   6. (Рекомендуется) В разделе Правила замени на настоящие правила для безопасности
      (подсказки в коде чата в app.js).

   Без конфигурации игра работает полностью, кроме онлай-чата.
   ========================================================================== */

// eslint-disable-next-line no-undef
window.FIREBASE_CONFIG = {
    apiKey: 'ВСТАВЬТЕ_API_KEY',
    authDomain: 'ВАШ_ПРОЕКТ.firebaseapp.com',
    databaseURL: 'https://ВАШ_ПРОЕКТ-default-rtdb.firebaseio.com',
    projectId: 'ВАШ_ПРОЕКТ',
    storageBucket: 'ВАШ_ПРОЕКТ.appspot.com',
    messagingSenderId: 'xxxx',
    appId: '1:xxxx:web:xxxx',
};